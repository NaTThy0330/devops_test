apiVersion: v1
kind: Service
metadata:
  name: api-service
  labels:
    app: devops-assignment
    component: api
    version: v1
spec:
  type: ClusterIP
  selector:
    app: devops-assignment
    component: api
    version: v1
  ports:
    - name: http
      port: 80
      targetPort: http
